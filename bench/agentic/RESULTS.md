# エージェントベンチ 実測記録(途中経過)

最終更新: 2026-07-20。方法論は [README.md](README.md)。生ログは results/*.json
(共有分のみ手動でコミット。`permissions` フィールドが違う結果を並べて比較しないこと)。

## 測定サマリ

| 日付 | アーム | 権限 | k | pass@1 | pass@k | 中央値 | 備考 |
|---|---|---|---|---:|---:|---:|---|
| 07-12 | fable-t-mid(30B+規律) | edit | 3 | 3.2% | 9.5% | 106s | ループ封じ条件 |
| 07-12 | raw-qwen(30B素) | edit | 3 | 3.2% | 9.5% | 116s | 同上・完全同点 |
| 07-12 | fable-t-mid(30B+規律) | **python** | 3 | 4.8% | **9.5%** | 100s | ループ許可 |
| 07-12 | raw-qwen(30B素) | **python** | 3 | 0% | **0%** | 129s | ループ許可 |
| 07-20 | fable-t-mid+改良規律v2 | python | 1×3タスク | 0/3 | — | — | 行動検証(下記) |
| 未測 | fable-t(120B+規律) | python | 3 | — | — | — | **g24 VRAM待ち**(他ユーザーのASR学習が65GiB占有中。120Bは空き70GiB必要) |
| 未測 | raw-gptoss(120B素) | python | 3 | — | — | — | 同上 |

## 07-20 の発見: 「検証ループは一度も回っていない」

python モード(検証ループ許可)の 126 試行(fable-t-mid 63 + raw-qwen 63)の作業
ディレクトリを精査した結果、**エージェントがテストファイルを書いた回数はゼロ**。
`*test*` に見えた3ファイルはタスク名 `dedupe-latest` の誤マッチだった。

- 失敗の型もこれと整合する:
  - `retry-backoff`: 「sleep を差し替え可能に保て」という明示要件を**削除**して全テスト TypeError
  - `csv-quotes`: 引用符処理を実装せず、**docstring を壊れた実装に合わせて書き換えた**
  - どちらも1回テストを走らせれば即座に気づく類のもの
- 配管は正常であることを直接確認済み: 同じ経路・同じ権限で「Bash で python を実行せよ」と
  明示指示すれば fable-t-mid は正しく実行し出力を返す(6*7→42 で確認)。
- つまり **fcc/権限/ツールの問題ではなく、モデルが自発的にループを回さない行動の問題**。
  python モードでの fable-t-mid 9.5% vs raw-qwen 0% の差も、ループの効果ではない
  (どちらも回していない)。

## 改善の試み(fable-coding.txt)

- v1(07-20): 末尾に「実行可能なら必ずテストを書き実行せよ」の段落を追記
  → 3タスクの行動検証で**変化なし**(テスト0、csv-quotes は docstring 改変を継続)
- v2(07-20): ファイル構成を再構築。**冒頭に命令形の MANDATORY VERIFICATION PROTOCOL**
  (要件チェックリスト→スクラッチテスト作成→実行→緑まで反復→実行ログを引用して報告)を
  配置し、「要件の削除・docstring の書き換えは修正ではない」を明文化
  → 行動検証を実行中(本コミット時点で途中)

## 次にやること

1. v2 の行動検証(csv-quotes / retry-backoff / interval-merge ×1)の結果確認
   — テストファイルが生成されるかが判定基準。合否より行動変化を見る
2. 行動が変われば fable-t-mid k=3 全21タスクを再測定し、raw-qwen(0%)と比較
3. g24 の VRAM が空き次第、本命の fable-t(120B) vs raw-gptoss(120B) を python モードで測定
4. プロンプトだけで行動が変わらない場合の代替案: ハーネス側でループを強制する
   (/office の検証器ファーストを -p 単発にも適用する薄いラッパ。ただしベンチの
   全アーム共通条件を崩さない設計にすること)
